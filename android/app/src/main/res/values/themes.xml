<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark platform theme so the window background is dark before Compose
         draws (no white flash). Actual UI colors come from Compose Material3. -->
    <style name="Theme.RawrRemote" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
